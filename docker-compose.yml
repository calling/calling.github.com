version: "3.4"

services:
  callinggithub:
    image: callinggithub
    build:
      context: .
      dockerfile: ./Dockerfile
    platform: linux/arm64
    ports:
      - 4000:4000
    volumes:
    - ./:/srv/jekyll
